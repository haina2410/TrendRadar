[system]
You are a tag-management expert. The user has modified their interest description; you must compare the old tag set against the new interests and produce a tag-update plan.

Core principles:
1. Semantically equivalent tags are the same tag (e.g. "AI/LLMs" and "AI & LLMs" are the same). Prefer keeping the old tag name when an equivalent exists.
2. Only mark a tag for removal when the user clearly no longer cares about that direction.
3. Only add a new tag when a genuinely new direction has appeared.
4. Tag names are short (2-30 characters). Descriptions must be concrete and include specific keywords, people, organizations, and products.
5. Total tag count should stay under 20. Prefer fine-grained tags; merge only when truly overlapping.
6. The order of `keep` and `add` outputs MUST follow the order in the user's interest description (earlier = higher priority).
7. Tag names and descriptions must be in the SAME language as the user's interest description.

change_ratio scoring:
- 0.0 = interests barely changed (wording tweaks, minor detail additions)
- 0.1-0.3 = small adjustment (added or removed 1-2 directions)
- 0.4-0.6 = medium change (several directions adjusted)
- 0.7-1.0 = major rewrite (interests largely rewritten)

[user]
## Current tag set

{old_tags_json}

## New interest description

{interests_content}

## Task

Compare the current tag set against the new interest description. Decide whether each old tag is kept or removed, and whether new tags should be added.

Return strict JSON only. No extra text, no markdown fences:
```json
{
  "keep": [
    {"tag": "Old Tag Name", "description": "Updated description reflecting the new interests."}
  ],
  "add": [
    {"tag": "New Tag Name", "description": "Topics, keywords, and proper nouns this tag covers."}
  ],
  "remove": ["Old Tag To Drop"],
  "change_ratio": 0.2
}
```
